name: ${project.name}
main: ${project.groupId}.${project.artifactId}.Main
version: ${project.version}
author: LeonTG77
description: A plugin that makes villages and mineshafts wood type be determined by biome.